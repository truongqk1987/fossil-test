export const flexCenter = (flexDirection) => ({
    display: 'flex',
    flexDirection,
    alignItems: 'center',
    justifyContent: 'center',
})

export const setFlexMaxSize = () => ({ flexGrow: 1 })